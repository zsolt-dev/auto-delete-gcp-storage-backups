{
  "name": "auto-delete-gcp-storage-backups",
  "version": "1.1.0",
  "description": "Zero configuration backup deletion script to automatically delete backups from Google Cloud Platform Storage buckets. Runs each time after a new backup is added to the bucket and keep hourly backups for the desired number of hours and keeps one daily backup for the desired number of days.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/zsolt-dev/auto-delete-gcp-storage-backups.git"
  },
  "keywords": [
    "google cloud platform storage",
    "object storage",
    "auto delete backups" 
  ],
  "author": "Zsolt Szabo",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/zsolt-dev/auto-delete-gcp-storage-backups/issues"
  },
  "homepage": "https://github.com/zsolt-dev/auto-delete-gcp-storage-backups#readme",
  "dependencies": {
    "@google-cloud/storage": "^5.19.1"
  }
}
